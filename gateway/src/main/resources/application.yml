spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      default-filters:
        - TokenRelay
      mvc:
        routes:
          - id: user-service
            uri: lb://user-service
            predicates:
              # proxy paths in user service
              - Path=/users/**
    openfeign:
      oauth2:
        enabled: true
        clientRegistrationId: okta